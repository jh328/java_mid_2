## Section 1: 제네릭(Generics)

* 제니릭 타입 Box<T> 클래스를 정의할 때는 타입을 결정하지 않고, 타입을 외부에서 받아서 쓸 수 있또록 T 와 같은 <타입> 매개변수를 사용한다.
* 제네릭 클래스를 실제로 사용하는 시점(객체를 생성하는 시점)에 `Box<Integer>`처럼 타입 인수를 전달하여 사용할 타입을 최종 결정한다.
* "타입 결정을 클래스 정의 시점이 아닌, 사용하는 시점으로 미루는 것"이 제네릭의 특징이다.

## 제네릭과 메서드의 비유
* 제네릭은 메서드의 매개변수와 인수 관계와 매우 유사하다.

* 매개변수 (Parameter) 값을 받기 위한 자리 또는 변수 (메서드 정의 시)
* 인수/인자 (Argument) 매개변수에 전달하는 실제 값 (메서드 호출 시)
* 메서드가 값을 매개변수로 받아 처리하듯, 제네릭은 타입을 매개변수로 받아 처리합니다.

## IntelliJ 유용한 단축키

### `Control + T` (Refactor This)

# 리팩토링 관련 기능 모음 메뉴를 엽니다.

* **6. Inline Variable (변수 합치기)
    * 불필요하게 나뉜 변수를 하나로 합쳐줍니다.
    * **커서 위치**: 합쳐질 변수명(아래 예시에서는 `object`)에 커서를 둡니다.

    ```java
    // [단축키 실행 전]
    Object object = objectBox.get();
    Integer integer = (Integer) object; 
    
    // [단축키 실행 후]
    Integer integer = (Integer) objectBox.get();
    ```

### `Control + O` (Override Methods)

* 부모 클래스나 인터페이스의 메서드를 재정의(Override)할 때 사용합니다.
* 재정의할 수 있는 메서드 목록이 나타납니다.

